# 更新日志(CHANGELOG)

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.4.1] - 2026-08-26

### 🔧 修复
- **框选时整屏变黑**:整屏半透明窗口会触发 Windows"全屏优化"独立翻页(绕过 DWM 合成,透明度丢失→整屏不透明黑)。框选改为**截图静止画方案**(截图工具同款):进入时截全屏快照,用不透明全屏窗口显示冻结画面供拖选,屏幕视觉上无任何变化,从根上绕开所有窗口合成路径;坐标上报也顺势简化为物理像素直出(免 DIP 换算)。

## [1.4.0] - 2026-08-26

### ✨ 新增
- **框选区域模式(galgame 对话框翻译)**:主窗口「框选区域」按钮或悬浮框「选区」钮进入框选(全屏暗化 + 拖拽高亮框,Esc/右键取消);选定后只识别该区域,**译文直接显示在悬浮框的展开面板里**(自动展开/收起、超长滚动),游戏画面零遮挡;悬浮框按钮变为「清选」可一键退出回到全屏识别;
- 区域内容指纹复用同样适用于选区(对话框文字没变时重复触发近零开销)。

### 实现备注
- 框选层用 Opacity 暗化 + 独立高亮框窗口(本机不合成 AllowsTransparency 分层窗口,绕开限制);
- 多显示器异 DPI 的选区边缘换算为近似(按选区中心所在屏缩放),单屏精确。

## [1.3.0] - 2026-08-26

### 🔥 变更(功能收敛)
- **移除"字幕底块"渲染模式**,只保留"背景采样覆盖"(取文本外围背景色原位替换,观感最自然);渲染方式下拉框与悬浮框切换按钮一并移除,界面更简洁;
- 悬浮框宽度收窄(剩 ▶ 立即翻译 / 自动开关 / 语言循环 / × 隐藏四钮)。

## [1.2.1] - 2026-08-26

### 🚀 提速
- **OCR 提速约 30~45%**:关闭逐行角度分类器(屏幕文字恒为横向,实测整屏 155 行省 ~700ms);onnxruntime 8 线程(det 1192→971ms);实测整屏识别 3513ms→1728ms(91 行);
- **区域指纹复用**:OCR 横带内容未变时跳过推理(静态页面反复触发近零开销);
- 修复:脏区全是自身 UI(主窗口/悬浮框)时不再退回全屏识别,直接跳过。

## [1.2.0] - 2026-08-26

### ✨ 新增
- **悬浮框快捷操作**:新增 ▶ 立即翻译(等同 Ctrl+Shift+T)、自动✓(开关自动触发,开启时蓝底高亮)、× 隐藏悬浮框三个按钮;主窗口改动语言/渲染方式时悬浮框按钮文字同步。

## [1.1.1] - 2026-08-26

### 🔧 修复
- **字体叠加**:字幕模式改为段落级整块渲染(覆盖段落行联合区域,译文整体换行、左对齐),消除密集对话框中逐行块膨胀互压;行间残句(如句尾被 OCR 拆出的"に。")一并盖住;
- 字号改为多行感知自适应(按行数与最长行宽双重约束 + 迭代收缩)。

## [1.1.0] - 2026-08-26

### ✨ 新增
- **悬浮状态框**:深灰半透明置顶小窗(可拖动、可关闭),实时显示翻译进度(识别/翻译/完成耗时/引擎/失败原因),两个快捷按钮直接切换渲染方式(字幕↔背景)和目标语言(中→英→日)——玩游戏时无需切回主窗口;

### 🚀 提速
- **并行翻译**:多源语言分组并行批量请求;段落拆回失配时跨段合并成一次批量逐行重试(此前每个失配段落一次串行 HTTP,长文档延迟线性堆叠);

### 🔧 修复
- **日文振假名碎片**:汉字上方注音小字被 OCR 成独立小行、翻译后漂成碎块——新增启发式抑制(行高<0.55×中位行高 + 紧贴正常行上方 + 水平重叠≥40% 判为注音);
- 悬浮框自身区域加入 OCR/脏区/覆盖层排除列表,杜绝"识别自身"。

## [1.0.0] - 2026-08-25

首个公开版本。

### ✨ 核心功能
- **看到即翻译**:屏幕上的外文(游戏/视频/网页/软件界面)自动识别并翻译成中文,译文以字幕底块精确覆盖在原文位置;
- **只翻变化区域**:64px 分块指纹前后帧对比,只重识别变化部分;鼠标点击/滚轮/键盘 300ms 防抖自动触发,`Ctrl+Shift+T` 手动触发;
- **多显示器支持**:逐显示器截图与覆盖,任意 DPI 组合下坐标精确对齐;
- **目标语言**:中文/英文/日文可选;自动过滤已是目标语言的内容与垃圾文本(网址、纯数字、短码等);
- **两种渲染方式**:字幕底块(默认,适配一切背景)/ 背景采样覆盖(取文本外围背景色,纯色背景原位替换);
- **LRU 翻译缓存(4096 条)**:翻过的句子零成本复用。

### 🔍 识别引擎(OCR)
- **主引擎 RapidOCR**(RapidOcrNet 4.0.2 + PP-OCRv6 small 多语模型,ONNX,模型已内置约 31MB):整屏高质量识别,真实置信度 + 四角点坐标,支持中/英/日(假名)。实测 2560x1600 整屏识别 116 行、中文零错字;
- **兜底 Windows.Media.Ocr**:系统自带零依赖,模型缺失/识别异常时自动切换;
- 诊断自检:`--selftest-ocr-rapid` / `--selftest-ocr-hybrid` / `--selftest-ocr-screen` 等。

### 🌐 翻译引擎(行级降级链)
- DeepLX(自部署,质量最好)→ **Edge 免鉴权接口**(`edge.microsoft.com/translate/translatetext`,无需注册,段落级换行保留)→ MyMemory → Echo 兜底;
- **段落聚合翻译**:OCR 行按几何关系聚合成段落,整段一次请求,上下文连贯;拆回失配时逐行重试;
- 行级降级:某一行失败只降级该行,翻译失败的行不画块(保留原文露出)。

### 🔧 本版修复(实机验证)
- 覆盖层 z-order:每次渲染后重新断言置顶,不再被后弹出的 Topmost 窗口(演示页/游戏)压住;
- OCR 区域改为全宽横带:修复脏区裁剪把长文本行横向切成碎片的"右半行翻译"问题;
- 覆盖层内容签名包含颜色:修复切换渲染方式后底色不刷新;
- 背景采样改为取文本外围一圈像素平均值,底色更柔和;
- 过滤修复:不再按"含冒号"一刀切误杀 `NPC: ...` 对话。

### ⚠️ 已知限制
- 独占全屏游戏 BitBlt 截到黑屏:请改无边框/窗口化;
- PP-OCRv6 字典不含韩文,韩文场景待补;
- DeepLX 需自行部署(`deeplx_windows_amd64.exe` 见发布说明/需自备)。
