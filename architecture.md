# 屏幕实时翻译软件 —— 架构设计文档

> 目标:监听屏幕变化 → 截图 → OCR 定位文本 → 翻译 → 在原文位置覆盖显示译文。
> 核心指标:端到端延迟 ≤ 1s;只遮挡文本区域,不遮画面;CPU 占用低;重复文本零重复翻译。

---

## 1. 总体架构

```
┌─────────────┐   ┌──────────────┐   ┌──────────┐   ┌──────────┐   ┌──────────────┐   ┌──────────────┐
│ 输入事件钩子 │──▶│ 变化检测/防抖 │──▶│  截图模块  │──▶│ OCR 模块  │──▶│  过滤/去重    │──▶│  翻译引擎层   │
│ (鼠标/键盘)  │   │ (区域 diff)   │   │ BitBlt/  │   │ WinOCR/  │   │ (长度/语言/   │   │ (队列+并发+  │
└─────────────┘   └──────────────┘   │ DXGI     │   │ RapidOCR │   │  忽略区域/缓存)│   │  缓存+降级)  │
                                     └──────────┘   └──────────┘   └──────────────┘   └──────┬───────┘
                                                                                             ▼
┌──────────────────────────────────────────────────────────────────────────────────┐  ┌──────────────┐
│                   置顶透明覆盖窗口(WS_EX_TOPMOST + LAYERED + TRANSPARENT)          │◀─│ 渲染调度器     │
│   背景抹除(主色填充 / inpaint / 字幕底块) → 译文绘制(对比色) → 双缓冲整体提交        │  │ (bbox 定位)   │
└──────────────────────────────────────────────────────────────────────────────────┘  └──────────────┘
```

**数据流**:事件触发 → 变化检测确认"哪变了" → 只截变化区域 → OCR 得 (text, bbox) 列表 → 过滤 → 翻译队列批量翻译 → 渲染层按 bbox 抹原文、写译文。

**核心原则(纠正一个常见误区)**:不要"全屏截图 + 全屏覆盖"。全屏覆盖会遮死视频/动画/游戏画面,且每次重绘全屏会闪烁。正确做法是**增量**:只对变化区域重 OCR、只画有译文的小块,其余像素完全透明。

---

## 2. 模块设计

### 2.1 触发与变化检测

| 触发源 | 实现 | 说明 |
|---|---|---|
| 全局输入钩子(主) | `SetWindowsHookEx(WH_MOUSE_LL / WH_KEYBOARD_LL)` | 鼠标点击、滚轮、按键都算"用户交互信号" |
| 定时轮询(兜底) | 每 300~500ms 对屏幕做一次分块哈希对比 | 覆盖"视频字幕、动画"这类无输入也在变的场景 |
| 手动热键 | 注册热键强制全屏重翻 | 兜底手段 |

**防抖**:收到输入信号后启动 300ms 定时器,期间新信号刷新定时器,到期才执行一轮"截图-识别-翻译"。防止拖动窗口时连续触发十几轮。

**区域 diff(关键优化)**:把屏幕切成 64×64 的块,每块存一个快速哈希(可用每块像素的降采样指纹)。对比上一帧,只把哈希变化的块合并成"脏矩形"(可多个),只对脏矩形截图+OCR。这一步能把每轮处理量从全屏降到 5%~20%,直接决定整机 CPU 占用。

### 2.2 截图模块

| 场景 | 方案 | 延迟 |
|---|---|---|
| 普通桌面/窗口应用 | `BitBlt`(GDI,兼容性最好) | 5~20ms |
| 游戏/高帧率/独占全屏 | DXGI Desktop Duplication API | 2~8ms,且能拿到每帧画面 |

- 高 DPI 下注意 `SetProcessDpiAwareness`(见 §8 坑列表)。
- 截出的位图同时供三处使用:OCR 输入、背景色采样、变化检测 diff。**一次截图,三处复用**,别截三次。

### 2.3 OCR 模块

**首选:PaddleOCR 系(PaddleOCRSharp / RapidOCR)**,弃用 Windows.Media.Ocr 为主力

- 对游戏 UI、艺术字、复杂背景、竖排的识别质量显著优于 Windows.Media.Ocr,幻觉(瞎猜乱认)明显更少——这是两阶段管线(检测 det + 识别 rec)与训练数据决定的;
- **PaddleOCRSharp**(.NET 版,与本机 `D:\米奇妙妙小工具\Toolbox` 内置的是同一引擎):返回结构自带坐标,`PaddleOcrResultItem { Text, Score, Rect, Points(四角点) }`,一次调用同时拿到文本+精确位置,直接喂覆盖层;CPU 单帧 50~200ms;
- **RapidOCR**(同模型 ONNX 封装):免 Paddle 运行库,跨语言(C#/Python/JS),集成最省事,模型 10~15MB;
- Windows.Media.Ocr 降级为"快速模式"备选(零模型、适合低延迟优先的场景)。

**降级策略**:默认 PaddleOCR(高精度);若用户开"低延迟模式"或识别延迟超标,切 Windows.Media.Ocr。

> **当前实现(第 9 轮)**:主引擎为 **RapidOCR(RapidOcrNet 4.0.2 + PP-OCRv6 small 多语模型,ONNX)**——无 PaddleOCRSharp 社区版的 `box sizes <100` 整屏限制,实测 2560x1600 整屏识别 116 行、中英混排无错字(合成图置信度 0.987~1.000);模型(det small ~10MB + rec small ~21MB + dict)随项目内置、构建拷贝到 `models/v6/`。`HybridOcrEngine` 负责调度:全部区域优先 RapidOCR,模型缺失/初始化失败/识别异常时自动回退 Windows.Media.Ocr 兜底。PaddleOCRSharp 降级为诊断自检(`--selftest-ocr-paddle*`)不再参与主流程。多语模型思路参考了 DangoTranslator(本地 PP-OCR det/rec/cls 模型按语言组织,含 japan/korean rec);PP-OCRv6 small 单模型已覆盖 Latin + CJK(含假名),韩文如需可再补 korean rec 模型 + dict 做引擎内多模型切换。
>
> 历史(第 8 轮):曾落地 Paddle/Windows 按输入规模分流(≤1.8MP 走 Paddle),因社区版整屏限制被 RapidOCR 方案取代。

**反幻觉三板斧**(幻觉=识别出原文里没有的字):

1. 置信度过滤:`Score < 0.6` 的行不显示;
2. 帧间交叉验证:同一位置连续两帧识别结果一致才上屏(游戏静止场景免费获得,抖动文本天然被过滤);
3. 术语词典校验:常见误识(如 `I`/`l`/`1`、`O`/`0`)按词表修正。

**定位原理(关键)**:OCR 管线本来就是"检测(det,找文本在哪)→ 识别(rec,认字)"两步,所以**任何正经 OCR 引擎都返回坐标**;只有"只输出纯文本"的封装才会丢坐标。若复用只给文本的外部工具(如 Toolbox),用"检测-识别分离"方案补定位:

1. 本地跑轻量文本检测模型(PP-OCRv4 det / DB / EAST,ONNX 仅数 MB)拿到所有文本框 bbox;
2. 每个 bbox 裁成小图,按"上→下、左→右"排序编号;
3. 逐张送外部工具识别(或按同样顺序拼图识别);
4. 识别结果按编号映射回 bbox → 精确定位完成。
   前提:外部工具的识别顺序与输入顺序一致;整图一次识别再按文本顺序对齐是**不可靠**的,必须逐框喂。

**输出数据结构**(统一接口,引擎无感切换):

```text
OcrResult {
  lines: [ TextLine ]
}
TextLine {
  text: string          // 该行文本
  bbox: Rect            // 屏幕坐标(注意 DPI 缩放换算)
  points: [4 x Point]   // 四点角坐标(PaddleOCR 原生,支持旋转文本)
  confidence: float     // 0~1
  language: string      // 可选,语言检测结果
}
```

### 2.4 文本过滤("哪些是文本")

OCR 引擎给出文本+位置后,还需要过滤掉不该翻的:

1. **长度过滤**:`len(text) < 2` 跳过(单个字母/数字/图标误识);
2. **语言过滤**:已检测为目标语言(如中文)的行跳过——用轻量语言检测(字符范围统计即可,不必上模型);
3. **置信度过滤**:`confidence < 0.5` 跳过;
4. **忽略区域**:可配置黑名单矩形(如自己程序的 UI、菜单栏);
5. **重复抑制**:文本哈希 → 若与上一帧相同且译文未变,跳过(防闪烁)。

### 2.5 翻译引擎层("包速度包量"的核心)

**引擎抽象**:所有引擎实现统一接口,可热切换、可并行、可降级:

```text
interface Translator {
  name: string
  translate(lines: string[], from: string, to: string): Promise<LineResult[]>
  // LineResult = { index, text, ok }  行级返回,失败行标记 ok=false
}
```

**引擎清单**:

| 引擎 | 速度 | 量(免费) | 质量 | 说明 |
|---|---|---|---|---|
| DeepLX(本地代理) | 快 | 大 | ★★★★★ | DeepL 免费接口的本地代理服务,质量最好,需自部署(一个 exe) |
| Google 非官方接口(googletrans / translators 库) | 快 | 大 | ★★★★ | 免费但有限流风险,需做退避 |
| 官方 API(Google/DeepL/微软) | 快 | 官方免费额度 | ★★★★~★★★★★ | 稳定,适合正式产品做兜底 |
| 离线 NLLB-200 + CTranslate2 | 中 | **无限** | ★★★(中文一般) | 彻底包量、零网络,吃显存/内存,适合离线游戏场景 |

**"包速度包量"的五件套(比选哪个引擎更重要)**:

1. **批量请求**:把一行拼成一句、一次请求翻多行(引擎按行返回),请求数降一个数量级;
2. **并发池**:同时 4~8 个请求在飞,单请求延迟被摊平;
3. **LRU 哈希缓存**:key = 源文本+语言对。游戏里同一句台词反复出现,缓存命中率极高——**命中缓存的行延迟为 0**,这是"包量"的最大功臣;
4. **降级轮换**:主引擎被限流/超时 → 自动切备用引擎,失败行标记后重试;
5. **结果合并**:部分行失败时,只重翻失败行,不整体重来。

**调度**:翻译队列按"屏幕位置从上到下"排序,保证用户先看到顶部文本(阅读顺序)。

### 2.6 覆盖渲染(置顶窗口 + 文本抹除)

**窗口属性**(创建即置顶,不依赖 PowerToys):

```text
WS_EX_TOPMOST      // 永远置顶
WS_EX_LAYERED      // 分层窗口,支持逐像素 alpha
WS_EX_TRANSPARENT  // 鼠标点击穿透(配合 WS_EX_LAYERED)
窗口本身:无边框、全屏、背景全透明、不抢焦点
```

**对每个 TextLine.bbox 的处理流程**:

1. **背景判定**:从截图中裁出 bbox 区域 → 计算颜色统计(均值/方差/主色)。方差低于阈值 → 判定"纯色背景"(对话框、UI、游戏对话框),走方案 A;否则走方案 B。
2. **方案 A — 主色填充**:用区域主色画一个比 bbox 略大 2~4px 的色块(留 padding 盖掉文字边缘抗锯齿),把原文"抹掉"。
3. **方案 B — 复杂背景**(图片里的字、视频字幕):
   - 首选:OpenCV `inpaint`(TELEA 算法)以 bbox 周边像素填充原文区域——效果好但每区域约 5~30ms;
   - 省事替代:**字幕式底块**——半透明黑色圆角矩形垫底再写白字(类似弹幕),兼容一切背景,视觉上可接受。
4. **文字颜色自适应**:按背景亮度 `luminance > 0.5 ? 黑 : 白`,保证可读性。
5. **字体缩放**:译文宽度 > bbox 宽度时,自动缩小字号或换行,保持不溢出。

**渲染细节**:

- 用 **Direct2D / WPF**,不要用 GDI 直接画到窗口(闪烁严重);
- **双缓冲**:所有色块和文字先画到离屏位图,一次 `UpdateLayeredWindow` / `Present` 整体提交,杜绝撕裂和闪烁;
- 覆盖窗口本身也要做 diff:只有译文变化的小块才重绘;
- 多显示器:窗口跨所有屏,坐标按各自 DPI 换算。

---

## 3. 性能预算(单轮)

| 阶段 | 预算 | 手段 |
|---|---|---|
| 变化检测 | ~5ms | 分块哈希,只处理脏矩形 |
| 截图 | 5~20ms | BitBlt / DXGI |
| OCR | 50~200ms | 系统 OCR 或 RapidOCR,只对脏区域 |
| 过滤+缓存 | <1ms | 哈希 |
| 翻译 | 100~500ms(网络,并发摊平) | 批量+并发池,缓存命中则 0ms |
| 覆盖绘制 | 5~20ms | Direct2D + 双缓冲 |

**端到端 ≈ 0.2~0.8s**,这就是行业水平(团子翻译器/LunaTranslator 同量级)。

---

## 4. 技术选型

| 维度 | C# + WPF/WinUI | Python + PySide6 |
|---|---|---|
| Windows.Media.Ocr 调用 | 原生,最顺 | winrt 包,可行 |
| 置顶透明窗口 | 原生支持,代码少 | pywin32 手工设置 WS_EX 属性,可行 |
| Direct2D 渲染 | 原生 | 需 QPainter/自绘,勉强够用 |
| OCR 备选 RapidOCR | 有 .NET 绑定 | 原生支持,最顺 |
| 开发速度 | 中 | 快(验证思路首选) |
| 打包分发 | 单 exe,干净 | 体积大(pyinstaller ~100MB+) |
| 适合阶段 | **正式产品** | **原型验证** |

**建议**:先用 Python 把"截图→OCR→覆盖→翻译"整条链路跑通验证效果(1 天内);验证可行后,正式产品用 C# + WPF 重写。

---

## 5. 开发路线图(里程碑)

- **M1 — 骨架(半天)**:全屏截图 + 置顶透明窗口 + 手动热键触发,窗口上能画任意色块/文字。
- **M2 — OCR 接入(1 天)**:OCR 返回 (text, bbox) → 覆盖层按 bbox 抹背景+写原文,肉眼验证对齐精度。
- **M3 — 翻译链路(1 天)**:翻译引擎抽象 + DeepLX + 并发池 + LRU 缓存,替换原文为译文。
- **M4 — 自动化(1 天)**:输入钩子 + 防抖 + 区域 diff,实现"动一下就翻、不动不翻"。
- **M5 — 打磨(持续)**:复杂背景 inpaint、多显示器/高 DPI、忽略区域配置、引擎降级、设置界面。

---

## 6. 已知坑与对策

| 坑 | 对策 |
|---|---|
| 高 DPI 下坐标错位(截图/OCR/窗口三套坐标系不一致) | 进程设 `SetProcessDpiAwareness(PER_MONITOR_AWARE)`,全程用物理像素 |
| WinRT OCR 在非 STA 线程抛异常 | 放到独立 STA 线程或 UI 线程跑 |
| 游戏独占全屏时 BitBlt 截到黑屏 | 切 DXGI Desktop Duplication,或提示用户改无边框窗口 |
| 翻译延迟导致"先原文后译文"闪烁 | 覆盖层先画"占位抹除块"隐藏原文,译文到了再写字;或对高频变化区域延迟显示 |
| 输入法打字过程中频繁触发 | 防抖窗口拉长到 500ms,且钩子过滤 IME 消息 |
| 密码框/私密内容被翻译上屏 | 默认忽略焦点窗口的 ES_PASSWORD 控件,提供"暂停翻译"热键 |
| 系统 OCR 不识别某些字体 | 置信度监控,自动切 RapidOCR |
| 覆盖层抢焦点 | 窗口不设置 WS_EX_NOACTIVATE 之外的焦点行为,创建时用 `WS_EX_NOACTIVATE` 彻底禁抢焦点 |

---

## 7. 结论(对应你的三个问题)

1. **如何覆盖**:置顶透明窗口 + OCR 给的 bbox + 背景主色填充(纯色)或 inpaint/字幕底块(复杂背景),增量绘制 + 双缓冲。PowerToys 不需要,窗口属性一行代码的事。
2. **如何识别文本**:Windows.Media.Ocr 是性价比之王(系统自带、带 bbox);追求中文质量切 RapidOCR;再加长度/语言/置信度/忽略区域四道过滤。
3. **如何包速度包量**:批量请求 + 并发池 + LRU 缓存 + 多引擎降级轮换是架构题;引擎本身 DeepLX 免费质量最好,离线场景 NLLB 无限量。
