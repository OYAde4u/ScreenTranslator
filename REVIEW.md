# ScreenTranslator 项目审查报告

> 审查日期:2026-08-24 · 范围:全部源码(Services 12 个模块 + 主窗口 + 翻译/OCR 管线)
> 核心结论:**"翻译效果不好"的根源不在翻译引擎本身,而在 OCR → 过滤 → 翻译这条链路上有 3 个 P0 级问题**,按本文优先级修完后质量会有质的飞跃。
>
> **进度(2026-08-25)**:路线图 1~4 项 + P1-5/P1-6/P2-9/P2-10 全部完成 ✅(P0-3 的 Google 接口因网络不可用改为 Edge 免费接口);代码屎山清理完成(App.xaml.cs 552→40 行,自检抽到 SelfTests.cs,删除死代码)。剩余:RapidOCR 替换 Windows OCR、帧间交叉验证、暂停热键、隐私提示。

---

## 一、P0:直接导致"翻译效果差"的三个根因

### 1. 逐行独立翻译,完全丢失上下文(质量损失最大)

**现状**(`DeepLXTranslator.cs:38-41`、`MyMemoryTranslator.cs:29-32`):

```csharp
var tasks = texts.Select((t, i) => TranslateOneAsync(t, i, from, to, ct, results)).ToArray();
```

每一行 OCR 文本**单独**发一个请求。而 OCR 是按"视觉行"切分的——一句完整的话经常被拆成 2~4 行:

```
原文:"I was wondering if you could" / "help me with this quest"
逐行翻:"我想知道你是否能" / "帮我做这个任务"   ← 破碎、生硬
整句翻:"我想知道你能不能帮我完成这个任务"        ← 自然
```

而且这与架构文档 §2.5 的设计**相悖**:架构明确要求"批量请求:把一行拼成一句、一次请求翻多行",实现里却完全没有批量,也没有上下文。

**优化方案**:
- **行 → 段落聚合**:翻译前把 OCR 行按几何关系(垂直间距 < 1.5 倍行高、左边界接近、同一脏区内)合并成段落;
- **整段一次请求**:段落内用换行符连接,一次请求返回后按换行符拆回各行(DeepLX/DeepL 对多行文本会保留换行);
- **上下文捎带**:把上一段文本作为 context 一起发(DeepL API 原生支持 `context` 参数,免费接口也吃多行上下文),游戏对话的代词、省略句质量提升明显。

### 2. 日/韩文内容被"语言过滤"误杀(日语场景完全不可用)

**现状**(`OcrLineFilter.cs:42-58`):

```csharp
// CJK 占比 > 12% 就视为"已是中文"
if (c >= 0x4E00 && c <= 0x9FFF) cjk++;
...
if (IsTargetLanguage(text, targetLang)) continue;  // 跳过不翻
```

`0x4E00~0x9FFF` 是 CJK 统一表意文字,**日文汉字、韩文汉字全落在这个区间**。目标语言设为 ZH 时:
- 日文句子(含大量汉字,如「冒険者ギルドへようこそ」)→ 被判为"已是中文"→ **直接过滤掉,永不翻译**;
- 混进来的日文还会污染源语言检测(`MainWindow.xaml.cs:191`:任一行为 CJK 则整批 from=ZH),英文行也被当中文源送去翻。

**这是游戏翻译场景的致命伤**——屏幕翻译器最典型的用途就是日/英文游戏,日文内容会成片消失。

**优化方案**:
- 语言检测改为三段判定:含**平/片假名**(`0x3040~0x30FF`)→ 日文;含**韩文音节**(`0xAC00~0xD7AF`、`0x1100~0x11FF`)→ 韩文;只有纯汉字+中文标点才判中文;
- 源语言不要整批共用一个值,**按行检测、按源语言分组**后再分别送翻译;
- 或者更省事:DeepLX/DeepL 传 `source_lang=auto`,让引擎自己判。

### 3. 主力引擎链实际只剩 MyMemory,质量天花板太低

**现状**(`MainWindow.xaml.cs:52`):`DeepLX → MyMemory → Echo`

- DeepLX 免费接口**限流严重(429)**,熔断后 60 秒内整批降级;
- MyMemory 是**翻译记忆库**而非神经网络翻译,短句/游戏文本质量差,免费额度约 5000 字/天,超限后返回警告文本;
- Echo 直接把**原文当译文画上去**——用户看到"块盖住了但内容没变",主观感受就是"翻译效果差"。

**优化方案**(按性价比排序):
1. **Google 免费接口**(`translate.googleapis.com/translate_a/single?client=gtx`,即 googletrans 用的那个):免费、量大、质量接近 DeepL,架构文档里已列为备选但没实现,补上它作为第二引擎;
2. **DeepLX 配 DeepL API Free Key**(50 万字符/月免费),解决 429;
3. **多引擎行级混合**:主引擎失败的行才降级,而不是整批降级(见下文 P2-2);
4. **Echo 兜底时不要画覆盖块**,改在状态栏提示"翻译服务不可用",不要用原文覆盖原文。

---

## 二、P1:OCR 质量链路(垃圾进 → 垃圾出)

### 4. Windows OCR 置信度恒为 1.0,反幻觉第一板斧失效

`WindowsOcrEngine.cs:117` 每行 `confidence: 1.0` 硬编码。架构设计的三道防线(置信度过滤 / 帧间交叉验证 / 术语词典)**一道都没生效**——`OcrLineFilter.Apply(lines, minScore: 0.5)` 形同虚设,识别错的乱码直接送翻译。

**方案**:
- 短期:用启发式补置信度——行高异常(<8px 或 >100px)、字符集中出现高比例生僻符号、宽高比异常的行降权;
- 根治:**换 RapidOCR**(PP-OCRv4/v5 的 ONNX 封装,有 .NET 绑定,无 PaddleOCRSharp 社区版"检测框 <100px"限制),真实置信度 + 幻觉少,架构文档本来就是首选它;
- 实现架构要求的**帧间交叉验证**:同一位置(±几 px)连续两轮识别结果一致才上屏,静止场景下几乎零成本,能滤掉大部分抖动幻觉。

### 5. 切块边界的"半行重复"会产生双重覆盖块

`WindowsOcrEngine.cs` 切块带 48px 重叠,但 `MergeLines` 只在"位置相近**且文本完全相同**"时去重。一行文本横跨块边界时,两个块各识别出**不同的半截**(如 "I was wonderi" / "wondering if you"),位置相近但文本不同 → 两行都保留 → 都翻译 → **两个覆盖块叠在一起**。

**方案**:去重条件改为"bbox IoU > 0.5 即视为同一行,保留文本更长/置信度更高的那个"。

### 6. 无意义文本浪费请求、污染译文

`IsGarbage` 只过滤纯符号。以下都会原样送去翻译:
- 纯数字行(伤害数值、金币数 "12345")——MyMemory 偶尔返回奇怪结果;
- 单字母+数字短码("A1"、"F5")、快捷键提示;
- URL、文件路径、含大量 `/\._` 的字符串。

**方案**:加三条规则——纯数字/纯标点跳过;字母数字混合且无空格且 <6 字符的短码跳过(可配开关);含 `://` 或路径分隔符密度 >30% 的跳过。

---

## 三、P1:渲染层——译文"显示了但看不清/不全"

### 7. 放不下的译文被省略号截断

`OverlayWindow.cs:145-156`:字号收缩到 8px 下限后,`TextTrimming.CharacterEllipsis` 直接把译文截成"…"。**用户看到的译文是不完整的**,这也是"效果不好"的重要观感来源。中文译文通常比英文原文短,但日译中、长句仍会溢出。

**方案**:
- `TextWrapping.Wrap` 允许换行,覆盖块高度自适应到 2~3 行(字幕底块风格下视觉完全可接受);
- 或翻译后处理:译文长度 > 原文 1.8 倍时请求引擎"用更简洁的说法重翻"(DeepL 短句模式),做不到再换行。

### 8. 译文块闪烁:位置签名过于敏感

`OverlayWindow.cs:112` 的内容签名包含坐标(`{i.X:F0},{i.Y:F0}`),OCR 框抖动 1~2px → 签名变 → 整个窗口 Canvas 清空重建 → 所有块闪一下。加上每轮 `HideAll → 35ms → 截图 → ShowAll`,静止画面也会周期性闪烁。

**方案**:
- 签名坐标量化到 4px 网格;文本未变的块不重建;
- 增量更新:维护"位置指纹 → 译文"映射,本轮识别出的行若文本与上一轮相同且位移 <8px,直接复用旧块,不重翻不重建(命中率在静止场景接近 100%)。

---

## 四、P2:健壮性与架构债

### 9. 整批失败语义 → 双倍延迟
`TranslationPipeline.TryTranslateAsync`:DeepLX 逐行并发时任一行失败 → `Task.WhenAll` 抛出 → **整批降级**到 MyMemory 全部重翻,一轮翻译花两倍时间。应改为行级收集:成功的行保留,只有失败的行进入下一引擎。

### 10. 缓存是 FIFO 不是 LRU
`TranslationPipeline.cs:71`:`_cache.Remove(_cache.Keys.First())` 依赖 Dictionary 枚举顺序,是 FIFO 而非 LRU。热台词可能被驱逐。换 `LinkedList + Dictionary` 的真 LRU,或直接用 `MemoryCache`。

### 11. OCR 块串行执行
`WindowsOcrEngine.RecognizeAsync` 逐块 `await`,全屏 6~9 块 × 每块数百 ms = 数秒。可并行(每线程一个 `OcrEngine` 实例,`TryCreateFromUserProfileLanguages` 很便宜),或脏区场景下本来就该只有 1~2 块——真正的问题是**首次/全屏场景**的体验。

### 12. 架构文档要求的其他缺口
- 无"暂停翻译"热键、无密码框保护(§6 坑表要求);
- 无忽略区域的用户配置(目前只硬编码排除 app 自身窗口);
- MyMemory 把屏幕文本明文发到境外服务器,**无隐私提示**——翻译银行/聊天窗口内容前应征得用户同意,建议首次启动弹隐私说明;
- `deeplx_windows_amd64.exe` 散放在项目根目录,建议归到 `tools/` 并在 README 写明部署步骤。

---

## 五、修复路线图建议

| 顺序 | 事项 | 预期收益 | 工作量 |
|---|---|---|---|
| 1 | 段落聚合 + 整段翻译(P0-1) | 译文通顺度质的飞跃 | 半天 |
| 2 | 日/韩语言检测修复 + 按行分组源语言(P0-2) | 日语场景从"不可用"到"可用" | 2 小时 |
| 3 | 接入 Google gtx 免费接口,Echo 兜底改提示(P0-3) | 摆脱 MyMemory 质量天花板 | 半天 |
| 4 | 译文换行自适应,取消省略号截断(P1-7) | 译文完整可见 | 1 小时 |
| 5 | RapidOCR 替换 Windows OCR + 恢复置信度过滤(P1-4) | 识别错字大幅减少 | 1 天 |
| 6 | 帧间验证 + 增量渲染(P1-4/P3-8) | 消除闪烁与幻觉 | 半天 |
| 7 | 纯数字/短码/路径过滤(P1-6)、半行去重(P1-5) | 减少垃圾请求 | 2 小时 |
| 8 | 行级降级、真 LRU、暂停热键、隐私提示(P2) | 健壮性 | 1 天 |

做完 1~4 项,"翻译效果"的主观感受就会有质的提升;5~8 项是把"能用"打磨成"好用"。

---

## 六、修复记录(2026-08-24,第 7 轮)

| 报告项 | 状态 | 说明 |
|---|---|---|
| 1 段落聚合 + 整段翻译 | ✅ 完成 | 新增 `Services/Translate/LineGrouping.cs`,管线按几何聚合段落、`\n` 连接一次请求、译文拆回;段落按源语言分组各自翻译 |
| 2 日/韩语言检测 + 按行分组 | ✅ 完成 | `IsChinese` 增加假名排除;新增 `IsJapanese`/`IsKorean`;`IsTargetLanguage` 支持 ZH/JA/EN;`DetectSourceLang` 段落级检测;`--selftest-filter` 验证"日文汉字不再误判为中文" |
| 3 Google gtx / Echo 兜底 | ⚠️ 部分完成 | **Google gtx 实测 429 不可用,未接入**(修正报告建议);Echo 兜底已改为"不画块 + 状态栏提示";引擎链保留 DeepLX→MyMemory→Echo,管道记录实际引擎 |
| 4 译文换行自适应 | ✅ 完成 | `OverlayWindow` TextBlock 换行 + Measure 块高自适应,取消省略号 |
| 5 半行去重/拼接 | ✅ 完成 | `WindowsOcrEngine.JoinSplits`:同水平线碎片(y 重叠>60%、x 相邻≤12px)拼接回整行 |
| 6 垃圾过滤 | ✅ 完成 | 纯数字/短码(<6 无空格含数字)/URL路径(:// 或 ≥2 斜杠或冒号) |
| 7 帧间验证/增量渲染 | ⏸ 未做 | 依赖连续轮次基线,留后续轮次 |
| 8 行级降级 | ⏸ 未做 | 熔断后 DeepLX 即时失败,实际延迟已受控;完整行级收集需改引擎接口,收益有限 |
| 8 真 LRU | ✅ 完成 | Dictionary + LinkedList 实现 |
| 8 暂停热键/隐私提示 | ⏸ 未做 | 体验增强项,后续轮次 |
| RapidOCR 替换 | ⏸ 未做 | 离线沙箱无法安装 ONNX 包;作为联网升级项 |

**验证**:自检 7/7(`--selftest`、`--selftest-diff`、`--selftest-translate`、`--selftest-ocr`、`--selftest-e2e`、`--selftest-filter`、`--selftest-demo`);演示页 5 行英文被识别并以中文译文覆盖(截图像素验证)。
