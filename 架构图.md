# 屏幕实时翻译软件 · 架构图(零基础版)

> 这份文档写给完全不懂编程的人:用大白话讲清楚这个软件"是什么、怎么工作、代码放在哪"。
> 配图:`architecture-diagram.png`(双击用看图软件打开,可放大)。

---

## 一、这个软件是干嘛的?

**一句话**:你屏幕上出现的外文(游戏台词、视频字幕、网页、软件界面),它会**自动识别并翻译成中文**,译文像字幕一样**浮在原文的位置上**,把原文盖住。

- 看英文游戏/视频时,不用切出去查字典,屏幕上的字直接变成中文;
- 从"看到外文"到"显示中文",大约 **0.2~0.8 秒**;
- 鼠标动一下、键盘按一下,它就自动开始干活(也可以按 **Ctrl+Shift+T** 手动翻一次)。

---

## 二、一张图看懂它怎么工作

![屏幕实时翻译软件架构图](architecture-diagram.png)

图里有 **8 步流水线**(从左到右):

```
① 用户操作 ──► ② 自动触发 ──► ③ 截屏 ──► ④ 变化检测 ──► ⑤ 文字识别(OCR) ──► ⑥ 过滤 ──► ⑦ 翻译 ──► ⑧ 覆盖渲染
 点击/滚轮/     300ms 防抖     拍下整个     只挑变了      认出:字 + 位置    只留值得翻      并发 + 缓存    译文盖住原文
   按键          才动手       屏幕         的部分                           的外文
```

用大白话走一遍:

1. **用户操作**:你点了鼠标、滚了滚轮、按了键盘 —— 软件知道"你正在看屏幕";
2. **自动触发**:连续操作时它不急着干活(防抖),等你**停一下**才动手,避免一顿狂刷;
3. **截屏**:把当前整个屏幕"拍张照";
4. **变化检测**:和上一张照片对比,**只有变了的地方**才处理(比如只有字幕变了,就不重翻整个屏幕,省电省性能);
5. **文字识别(OCR)**:让电脑"看懂"照片里的字,并且**记住每个字在屏幕的哪个位置**;
6. **过滤**:挑出值得翻的 —— 太短的、已经是中文的、网址之类的,直接跳过;
7. **翻译**:把外文交给翻译服务,一次批量翻很多行;**翻过的句子会记进缓存**,下次再出现直接抄答案(0 延迟);
8. **覆盖渲染**:在原文的位置上盖一层"字幕底块 + 中文译文",像弹幕一样浮在屏幕上。

**翻译失败怎么办?** 4 家翻译服务排队干活:DeepLX(质量最好)→ Edge(免费)→ MyMemory(免费)→ Echo(兜底)。前面失败了自动换下一家;全部失败就原文显示,并在状态栏提示。

---

## 三、代码放在哪?(目录结构)

```
D:\ScreenTranslator\              ← 整个项目文件夹
├── ScreenTranslator\             ← 软件的全部源代码(主目录)
│   ├── App.xaml / App.xaml.cs    ← 程序的"启动按钮":开机从这里开始
│   ├── MainWindow.xaml(.cs)      ← 主界面 + 总调度:你看到的设置窗口,
│   │                               也是把 8 步流水线串起来的总管
│   ├── Services\                 ← 六大"车间",各干各的活:
│   │   ├── Ocr\                  ←   车间⑤ 文字识别(看懂字 + 定位)
│   │   │     ├── IOcrEngine.cs          识别引擎的"统一插头规格"
│   │   │     ├── WindowsOcrEngine.cs    Windows 自带的识字引擎(当前主力)
│   │   │     ├── PaddleOcrEngine.cs     百度 PaddleOCR 引擎(备用)
│   │   │     ├── OcrLine.cs             识别结果:一句话 + 它在屏幕的位置
│   │   │     └── OcrLineFilter.cs       过滤器:哪些字值得翻
│   │   ├── Translate\             ←   车间⑦ 翻译(外文 → 中文)
│   │   │     ├── ITranslator.cs         翻译服务的"统一插头规格"
│   │   │     ├── TranslationPipeline.cs 翻译总管:查缓存→并发→失败换下一家
│   │   │     ├── DeepLXTranslator.cs    翻译服务1:DeepLX(质量最好)
│   │   │     ├── EdgeTranslator.cs      翻译服务2:Edge 免费接口
│   │   │     ├── MyMemoryTranslator.cs  翻译服务3:MyMemory 免费接口
│   │   │     ├── EchoTranslator.cs      翻译服务4:兜底(原样返回,不卡流程)
│   │   │     └── LineGrouping.cs        把被拆成多行的句子拼回整段再翻译
│   │   ├── ScreenCaptureService.cs ← 车间③ 截屏(拍下整个屏幕)
│   │   ├── ScreenDiff.cs           ← 车间④ 变化检测(对比前后两帧)
│   │   ├── OverlayManager.cs       ← 车间⑧ 覆盖层管理(每个显示器一张"玻璃纸")
│   │   ├── OverlayWindow.cs        ←   一张透明置顶的"玻璃纸"窗口
│   │   ├── OcrOverlayRenderer.cs   ←   在玻璃纸上画"底块 + 译文"
│   │   ├── AutoTriggerService.cs   ← 车间② 自动触发(300ms 防抖)
│   │   ├── HotKeyService.cs        ←   热键 Ctrl+Shift+T
│   │   ├── InputHookService.cs     ←   全局监听鼠标键盘
│   │   ├── DisplayLayout.cs        ←   显示器信息(多屏坐标换算)
│   │   └── FrameOps.cs / PixelFrame.cs / Diag.cs ← 小工具(像素处理 / 日志)
│   ├── SelfTests.cs                ← 自检程序(验证各模块是否正常)
│   └── ScreenTranslator.csproj     ← 工程"配方"(用什么语言、依赖哪些库)
├── dot.ps1                         ← 一键构建脚本(把源码打包成 exe)
├── architecture.md                 ← 给开发者的架构设计文档
├── PROGRESS.md                     ← 开发进度记录
└── REVIEW.md                       ← 代码审查记录
```

**每个环节 → 对应的代码文件**,一眼对照:

| 环节 | 干的事 | 代码文件 |
|---|---|---|
| ① 用户操作 | 监听鼠标/键盘,响应热键 | `InputHookService.cs`、`HotKeyService.cs` |
| ② 自动触发 | 300ms 防抖,动一下才翻 | `AutoTriggerService.cs` |
| ③ 截屏 | 拍下整个屏幕 | `ScreenCaptureService.cs`、`PixelFrame.cs` |
| ④ 变化检测 | 只挑变了的部分 | `ScreenDiff.cs`、`FrameOps.cs` |
| ⑤ 文字识别 | 认出字 + 位置 | `WindowsOcrEngine.cs`、`PaddleOcrEngine.cs`、`IOcrEngine.cs` |
| ⑥ 过滤 | 留下值得翻的外文 | `OcrLineFilter.cs`、`OcrLine.cs` |
| ⑦ 翻译 | 并发翻译 + 缓存 + 降级 | `TranslationPipeline.cs`、`LineGrouping.cs`、4 个 `*Translator.cs` |
| ⑧ 覆盖渲染 | 译文浮在原文上 | `OverlayManager.cs`、`OverlayWindow.cs`、`OcrOverlayRenderer.cs` |
| 总调度 | 把 8 步串成流水线 | `MainWindow.xaml(.cs)` |
| 启动 | 程序开机入口 | `App.xaml(.cs)` |

---

## 四、几个名词,大白话解释

| 名词 | 大白话 |
|---|---|
| **OCR** | Optical Character Recognition,让电脑"看懂"图片里的字,还能告诉你字在图片的哪个位置 |
| **覆盖层** | 一层透明的"玻璃纸"盖在整个屏幕上;平时全透明、不挡鼠标,只在译文位置写字 |
| **防抖** | 连续操作时不立刻反应,等你"停一下"再干活,避免疯狂重复触发 |
| **变化检测(diff)** | 前后两帧对比,只处理"变了的那几块",省时省性能 |
| **缓存(LRU)** | 翻过的句子记下来,再次出现直接抄答案,不重复花钱花时间翻译 |
| **降级** | 主翻译服务挂了,自动换备用服务,保证软件不卡死 |
| **批量翻译** | 一次请求翻译很多行,而不是一行一行慢慢来,速度翻倍 |
| **段落聚合** | 一句话被识别拆成好几行时,先拼回完整句子再翻译,译文更通顺 |

---

## 五、技术底子(给想知道的人)

- **语言/框架**:C# + WPF,.NET 8,Windows 桌面程序(Windows 10/11);
- **识别引擎**:Windows 自带 OCR(主力,零依赖)+ 百度 PaddleOCR(备用);
- **翻译服务**:DeepLX(本地代理)→ Edge 免费接口 → MyMemory → Echo 兜底;
- **核心优化**:只翻译"变化区域"、只画"有译文的小块",保证 CPU 占用低、不遮画面、不闪烁;
- **构建**:运行 `dot.ps1` 一键打包,输出 `ScreenTranslator.exe`。

> 配图由 `render-diagram.ps1` 脚本自动生成;排版由 `check-diagram.ps1` 自动校验。
